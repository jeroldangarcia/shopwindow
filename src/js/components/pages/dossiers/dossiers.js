import React from 'react';
import { Page } from '../../layout/page/page';

const Dossiers = (props) => {
  return (
    <Page title="DOSSIERS" icon="content_copy" toggleDrawer={props.toggleDrawer}>DOSSIERS</Page>
  );
};

export default Dossiers;
